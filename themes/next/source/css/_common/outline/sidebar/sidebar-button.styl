.sidebar .sidebar-button {
  &:not(:first-child) {
    margin-top: 15px;
  }

  button {
    button($white);
    border: 1px solid $white;
    border-radius: 4px;

    i[class^='fa'] {
      margin-right: 5px;
    }
  }
}
